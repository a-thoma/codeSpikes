#include <stdio.h>
#include <string.h>

int main(int argc, const char *argv[]) {
	
	char *tokens[50];
	size_t n = 0;

	for (char *p = strtok(line, " "); p; p = strtok(NULL, " ")) {

	
	}

	return 0;
}